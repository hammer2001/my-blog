---
// src/pages/posts/[...slug].astro
import { getCollection, render } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PostHeader from '../../components/PostHeader.astro';
import TOC from '../../components/TOC.astro';
import Comments from '../../components/Comments.astro';
import FontSizeToggle from '../../components/FontSizeToggle.astro';
import ImageLightbox from '../../components/ImageLightbox.astro';
import PasswordGate from '../../components/PasswordGate.astro';

export async function getStaticPaths() {
  const posts = await getCollection('posts');
  return posts.map(post => ({
    params: { slug: post.id },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content, headings } = await render(post);
---

<BaseLayout title={post.data.title} description={post.data.summary || post.data.title}>
  {post.data.categories.includes('手记') && <PasswordGate />}
  <article class="max-w-5xl mx-auto px-4 py-12">
    <div class="flex gap-8">
      <div class="min-w-0 flex-1 max-w-3xl mx-auto">
        <PostHeader post={post} />
        <div class="flex justify-end mb-2">
          <FontSizeToggle />
        </div>
        <div class="prose">
          <Content />
        </div>

        <!-- 版权声明 -->
        <div class="mt-12 p-4 rounded-lg bg-warm-100 dark:bg-warm-850 border border-warm-200 dark:border-warm-700 text-sm text-warm-500 dark:text-warm-400">
          <p>&copy; {post.data.date.getFullYear()} 个人网站. 转载请注明出处。</p>
        </div>

        <Comments />
      </div>

      <TOC headings={headings} />
    </div>
    <ImageLightbox />
  </article>
</BaseLayout>
